# Revision Log

## 2026-05-11
- **Implementasi Admin Dashboard MVP** sesuai `docs/admin-dashboard-plan.md`:
  - Menambahkan route `/login` khusus admin dengan UI yang mengikuti palet dan tipografi di `docs/style.md`, plus metadata `robots` `noindex, nofollow`.
  - Menambahkan server action login/logout admin berbasis Supabase Auth (`app/login/actions.ts`) dengan validasi server-side terhadap role `profiles.role` (`admin` / `super_admin`) serta fallback whitelist `ADMIN_EMAILS`.
  - Menambahkan utilitas admin baru (`utils/admin.ts`) untuk session lookup, validasi identitas admin, dan guard terproteksi `requireAdminUser()`.
  - Menambahkan route `/admin` terproteksi untuk monitoring calon santri, berisi summary cards, search, filter status tes, filter rentang tanggal, sorting, pagination, dan tabel data operasional.
  - Menambahkan aksi operasional admin pada dashboard untuk update `status` dan `catatan_admin` langsung dari tabel pendaftar.
  - Menambahkan export CSV melalui route `app/admin/export/route.ts`.
  - Menambahkan pencatatan audit untuk login sukses, logout, export CSV, dan update status/catatan ke tabel `admin_audit_logs`.
  - Menambahkan `middleware.ts` serta helper `utils/supabase/middleware.ts` untuk refresh session dan memblokir akses `/admin` tanpa session.
  - Menambahkan `app/robots.ts` untuk `Disallow: /login` dan `Disallow: /admin`.
  - Menambahkan empty state dan error state ramah-admin pada `/admin` agar mismatch konfigurasi Supabase / RLS tidak gagal diam-diam.
- **Catatan implementasi penting**:
  - Schema yang terdeteksi pada project `nusa-registration` sudah memiliki `profiles`, `registrations`, `student_tests`, dan `admin_audit_logs`, sehingga implementasi app difokuskan pada reuse tabel existing.
  - Dashboard memakai `nama_lengkap`, `nomor_whatsapp`, status pendaftaran, dan progres tes sebagai field utama karena schema reachable tidak menyediakan relasi email pendaftar di `registrations`.
  - Ditemukan mismatch antara ref Supabase pada `.env.local` (`hvdztetsmjxosxcyswuw`) dan project management yang bisa diaudit (`pccxuptxegrgdiiwghwl` / `nusa-registration`), sehingga halaman admin kini menampilkan pesan diagnosis konfigurasi jika query gagal.

## 2026-04-15
- Membuat folder dokumentasi khusus untuk fitur promo banner hero.
- Mengganti nama file plan menjadi `hero-promo-banner-plan.md` agar lebih representatif.
- Menetapkan keputusan final bahwa promo banner tidak menambah CTA baru.
- Menetapkan CTA pendaftaran lama tetap dipakai, dengan copy diperbarui menjadi lebih selaras dengan promo.
- Menetapkan countdown menggunakan placeholder target agar implementasi tidak memakai deadline palsu.

## 2026-04-16
- Menyelaraskan semua file `.md` di folder `docs` dengan update primary brand menjadi `#2C8970`.
- Mengubah peran `#42CDBA` dari primary lama menjadi bright secondary support untuk menjaga nuansa tetap cerah.
- Memperbarui dokumen hero color redesign dan promo banner agar konsisten dengan warna logo terbaru.
- Memperbarui panduan typography brand: `Righteous` untuk tulisan logo `NUSA` dan `Work Sans` untuk tulisan `Boarding School`.
- Menetapkan `Work Sans` sebagai font utama website, dengan `Romulo Italic` dipertahankan sebagai accent font terbatas untuk tagline atau frase pendek.
- Melakukan eksekusi sesuai dengan `color-migration-plan.md` di seluruh komponen untuk menyesuaikan dengan `style.md`.
  - Migrasi seluruh penggunaan `#e3b251` dan `#E3B251` menjadi tipe `Accent` resmi yaitu `#F3B233`.
  - Mengubah generic tailwind background color dan warna section terang menjadi variasi `Surface` baru (`#F0FAF7`).
  - Mengganti icon, border, dan list styling dari warna `B6CB6C` yang dihapus menuju `Secondary` resmi (`#42CDBA`).
  - Menerapkan `Dark Base` resmi `#134146` di seluruh subheadline, body teks terang, dan panel background footer.
  - Memperbaiki ketidakkonsistenan metadata tahun ajaran di layout utama (mengubah ke 2026-2027).
  - Menyelaraskan CSS tokens di `app/globals.css` dengan `style.md` dan menghapus CSS tidak terpakai `styles/globals.css`.
  - Memperbarui komponen `hero-section.tsx` untuk menggunakan palet warna Primary `#2C8970`, memperbaiki terminologi object `COLORS`, dan mengatur ukuran teks/button agar lebih seimbang dan proporsional.
  - Melakukan perombakan estetika V2 pada `hero-section.tsx` menambahkan 2 Floating Badges (ikon Code & Star), mengubah badge Kuota ke atas H1, menipiskan margin `promo-banner.tsx`, dan menerapkan efek visual *Glassmorphism* (blur semitransparan) pada elemen tombol sekunder untuk kesan *tech academy* modern.
  - **Redesain Tagline (`nusa-tagline.tsx`)**: Merekapitulasi `COLORS` _hardcoded_ menjadi abstraksi statis; mengubah latar kosong menjadi latar *Surface* `#F0FAF7`; membubuhi *Glow Point* dan Watermark Kutipan Raksasa (`Lucide Quote`); meratakan penambahan *divider* horizontal kuning; serta mengkalibrasi lonjakan ketebalan kurva _responsive typography_ dari *Mobile* ke *Desktop* secara optimal.
  - **Revisi Tagline Final (Minimalisme Rushd)**: Menghapus kembali latar belakang grid/glow untuk menyelaraskan murni dengan kemewahan *white space* (Latar Putih Polos). Frasa puisi digabung ke dalam satu tarikan napas `<h2>` raksasa dengan utilitas `whitespace-nowrap` yang dipisah dua bagian (*Muslim Tangguh,* & *Jago IT.*) demi rasio anti-overflow pada HP layar kecil. Konversi *Subteks* menjadi `<h3>` berstandar lolos aksesibilitas terang (WCAG Contrast).
  - **Redesain Why Choose (`why-choose-section.tsx`)**: Transplantasi kode dari desain kubus *flat* ke *Tech Academy Edutech*. Latar belakang disokong oleh pola jaring mikro linier (Grid cyan opacity 6%) serta pendaran radial belakang *soft glow*. Modul daftar (`LI`) dibebaskan dari *hardcoded* lintasan vektor panjang diganti via integrasi `<CheckCircle2 />` dan `<Rocket />` besutan `lucide-react`. Kartu diringankan menggunakan trik Glass *group hover* translasi maya dan pantulan bayangan blur `hover:shadow-2xl`.
  - **Redesain Kurikulum (`curriculum-section.tsx`)**: Merendam latar belakang penuh ke warna *Surface* (`#F0FAF7`) untuk separasi zona dari warna dasar putih. Menyuntikkan Slogan Pengaman `"Muslim Tangguh, Jago IT"` dengan `Romulo Italic` + `whitespace-nowrap` di dalam subdeskripsi. Menyulap dimensi kartu kurikulum menggunakan *Bento-Card layout* yang merespons harafiah sentuhan *hover*, dipadukan dengan *Aspect Ratio Lock (4/3)* pada fitur *Framed Image*. Tuntas menghancurkan unicode kuno menjadi panah cerdas `<CheckCircle2>` dan `<MoveRight>`.
  - **Redesain Program 100 Hari (`program-100-days.tsx`)**: Mengamankan latar belakang eksterior dengan Putih demi kontras, sambil menyuntik kurva *Padding* vertikal besar `py-24`. Merombak kotak gradien membosankan menjadi *Sleek Tech Dark Mode Card* berlapis *grid texture blend* halus dengan kurva luwes kekinian `rounded-3xl` berbayang `shadow-2xl`. Merobek belenggu tanda kutip ganda dari Headline; memompa besaran teks ke eselon Monumental `6xl`; dan memoles *punchline* "Besoknya Gajian" menggunakan pesona harafiah `COLORS.accent` dan `font-romulo-italic`. Pada komponen anak (Grid), membakar memori warna "kuning Lego" lawas menjadi ornamen cincin emas pendar di dalam tempurung *Glassmorphism* blur (`backdrop-blur-md`).
  - **Redesain Tahapan Jurusan (`program-section.tsx`)**: Mengeskalasi H2 ke tingkat raksasa `6xl` dan mengkonversi redaksi judul bertema deklaratif ("Peta Perjalanan"). Menghancurkan `svg polyline` kumuh di Modul Metode Belajar dengan `CheckCircle2` dari Lucide. Memutakhirkan arsitektur kotak sempit menjadi *Giant Bento Card* membulat `rounded-3xl` berpembatas pudar `hover:border-[#42CDBA]/30`. Pada segmen linimasa, node diperlebar ke `w-12 h-12` disertai *gradient line*, dan menyuntikkan *bug-fix absolut spasial* (`left-6`) untuk mencegah garis bocor pada perangkat *Mobile*. Terakhir, membantai titik bullet kotor dengan vektor `<Code>` dan `<Palette>` dalam wujud *Inner Bento* Jurusan.
  - **Redesain Tim Pengajar (`teaching-team-section.tsx`)**: Mengamankan brand NUSA dengan font `Righteous` murni di badan Header yang telah dipompa menjadi skala raksasa. Menjaga simetri batas tepi luar `.container` dengan mendaratkannya ke spesifikasi universal `max-w-6xl`. Menghancurkan antarmuka kubus lawas menggunakan format pelentur mandiri (Flex Wrap) yang menyusun profil Guru menjadi formasi natural 3-atas dan 2-bawah di layar raksasa. Memoles Avatar foto dengan lingkaran `ring-4` dinamis warna `accent`, lalu merevisi seluruh skala tipografi *(Nama & Jabatan)* dari penyakit *kerdil* menjadi `text-xl/text-base` berkapasitas eselon menengah Apple-like. Totalitas desain di-inject tanpa memotong satu karakter data silabus pun dari *source code* murni Ustadz.
  - **Redesain Galeri Acara (`gallery-section.tsx`)**: Membersihkan kotoran linter bawaan *(unused import `next/image`)* terbangkalai. Melestarikan bentangan **Panoramic 2-Kolom** (Container `max-w-7xl` & Grid `md:grid-cols-2`) demi membebaskan ukuran gambar tampil raksasa dan megah di layar lebar. Menurunkan skala tipografi nama pameran (Dalam Card) ke kelas santai `text-lg md:text-xl font-semibold` agar tidak mencaplok kecantikan wajah foto. Menghapuskan filter overlay kuning murahan yang merusak *tone* foto kulit, menggantinya dengan gradasi peneduh estetik bertema `DarkBase (#134146)`. Melindungi aksesibilitas pembaca *Mobile / HP* dengan memastikan semua judul dan deskripsi foto tampil utuh statis terbaca jelas tanpa perlu harus di-hover sentuh (Fitur _Hover Mode Ninja_ dilimitasi eksklusif hanya untuk pergerakan kursor Layar Dekstop `lg:hover`). Terakhir, merendam tepian terluar sangkar galeri menggunakan ilusi kaca canggih *(Border Tech Glass)* yang menembakkan laser `neonCyan (#42CDBA)` saat foto dilewati kursor.
  - **Redesain Testimoni (`testimonials-section.tsx`)**: Meruntuhkan kerangka karosel lawas yang menjepit (*layout squeeze*) area kartu di mode *mobile*. Tombol arah kiri/kanan dipindah dan disatukan (*Clustered Navigation*) dengan paginasi titik ke dasar tengah komponen bergaya antarmuka minimalis *Apple-like*, membebaskan lebar kanvas 100% untuk deretan ulasan. Mengupgrade fisik ulasan menjadi *Giant Bento-Glass* berbantalan lega `p-10 rounded-3xl` lengkap dengan fisika levitasi gawai `hover:-translate-y-2`. Permukaan latar solid ditimpa dengan jaring energi digital *Cyber Mesh* halus. Terakhir, mengeksekusi operasi penumpasan pamali atribut warna absolut: Membinasakan `Border-Top` ulasan berwarna Neon Cyan menjadi konstitusi `DarkBase 12%`, melunturkan warna label jabatan santri dari `Primary` ilegal menuju `DarkBase 70%`, serta mengganti penyebutan cacat `Pure White (#FFFFFF)` mendadak menjadi murni Putih Premium `COLORS.white (#F7F7F2)` di sekujur nafas teks seputar komponen.
  - **Redesain Informasi Biaya (`fee-info-section.tsx`)**: Meruntuhkan format brosur kaku menuju antarmuka *Bento UI Tech Academy*. Seluruh hamparan kanvas diselamatkan dari putih hampa `#FFFFFF` menjad varian sejuk `COLORS.surface` (`#F0FAF7`). Modul harga pendaftaran dibongkar dari orientasi vertikal kerdil menjadi *Horizontal Premium Banner* yang melintang selebar grid. Tiga tulang punggung kartu harga (Uang Masuk, dsb.) disulap menjadi *Premium Bento Card* `rounded-3xl` berbekal daya angkat `hover:-translate-y-2`. Khusus array Biaya Masuk, disuntikkan **Skema Promo FOMO 10 Pendaftar**: Menurunkan angka `20 Jt` menjadi harga abu-abu tercoret *(Strikethrough)*, dan meresmikan harga diskon raksasa `12 Jt` di bawah bayang-bayang lencana Urgensi Oranye berkelip dinamis *(Pulse Animation)*. Terakhir, seluruh teks turunan *(Checklists)* disegel aman di ukuran `text-sm`, kotak memo "Informasi Penting" diekstraksi ke pilar tengah bawah secara sentris, dan tipografi usang "jt" ditertibkan menjadi lebih estetik "Jt".
  - **Redesain Partner Industri (`partner-section.tsx`)**: Menghancurkan desain brosur statis kerdil dan menggantinya dengan antarmuka B2B tingkat *Tech Academy*. Jarak panggung diekskalasi ke ukuran wajar `py-24 md:py-32`, dan latar dijinakkan menjadi `COLORS.white` eksklusif. Deretan logo partner dikumpulkan dan disejajarkan ke dalam wadah agung berbentuk dermaga kaca tembus pandang (*Glass Tech Dock / Kapsul*) dengan radius halus `rounded-[3rem]`, polesan `backdrop-blur` dan sedikit *shadow cyan*. Pada logonya sendiri ditanamkan sihir interaksi B2B murni: Mode pasif diubah membisu elegan memakai filter `grayscale opacity-50`, yang akan mendadak menyala memancarkan warna aslinya saat disapu kursor pendaftar *(Hover to Color)*. Skala *margin* dan dimensi logo `h-10 md:h-12` dikalibrasi secara matematis untuk menghindari efek logo *oversized* (Baliho) maupun tubrukan jarak hampa berlebih.
  - **Redesain Ringan Pendaftaran (`registration-section.tsx`)**: Menjalankan mode sentuhan ringan *(Lightweight Enhancement)* murni setelah operasi makro dibatalkan. Mengamankan seluruh ukuran asli tulisan, susunan spasi tipografi, dan format Grid orisinal agar tata letak suci tak terganggu. Fokus perombakan dibatasi secara mutlak hanya pada kulit kapsul, yang mana sudut kotaknya telah dihaluskan menjadi kelas premi `rounded-3xl` dengan efek bayangan berlapis ala B2B. Selain itu, tekstur antarmuka dipertajam dengan injeksi "panah menari" *(Micro-Hover Interaction)* pada tombol aksi "Daftar Sekarang", dibarengi volume bantalan tombol yang disempurnakan (`py-8 font-bold`) demi mengangkat tingkat *Click-Through Rate*.
  - **Redesain Footer Dasar (`footer.tsx`)**: Area bawah ini dicuci bersih dari dosa *Hardcoded Hex* (`#134146`, `#F3B233`) dan menggantinya memakai variabel pelindung `COLORS.darkBase` serta `COLORS.accent`. Perhitungan CSS primitif seperti `rgba(0.06)` dihabisi untuk kemudian dinaturalisasi memakai kelas cepat `bg-white/5` murni. Elemen Peta Google *(Iframe)* yang dulunya telanjang, kini diperangkap utuh-utuh ke dalam kapsul estetika *Glassmorphism* (`backdrop-blur-md rounded-2xl`) demi menyuntikkan sentuhan layar B2B tingkat atas. Pada antarmuka petanya, ditautkan interaksi misterius: Peta membisu abu-abu *(Grayscale)* saat didiamkan, kemudian meledak tajam dengan warna nyata tatkala kursor pendaftar menyentuh permukaannya *(Hover: Grayscale-0)*.

## 2026-04-19
- **Revisi Struktur Form Pendaftaran & Database**:
  - **Database Migration**: Melakukan truncating data dummy di tabel `registrations`. Menghapus kolom lama (`nama`, `no_whatsapp`) dan menambahkan struktur kolom baru: `nama_lengkap`, `nomor_whatsapp`, `tempat_lahir`, `tanggal_lahir`, `asal_kota`, `alamat_lengkap`, `sekolah_asal`, `lokasi_sekolah`, dan `sumber_informasi`.
  - **Pembaruan Zod Schema & React Hook Form**: Mengubah skema validasi Zod agar sesuai dengan field baru beserta penanganan error pesannya (termasuk validasi tambahan seperti regex untuk `nomorWhatsapp` yang wajib diawali '62').
  - **Redesain UI Form (`registration-form-page.tsx`)**: Merombak tampilan list field menjadi 4 seksi terkelompok (Card-Group Layout): **A. Informasi Pribadi**, **B. Informasi Sekolah**, **C. Tambahan Informasi** (termasuk dropdown *sumber informasi* dengan icon dropdown kustom), dan **D. Pembayaran Pendaftaran**.
  - **Pemolesan UX**: Menambahkan visual statis `+62` di input nomor WhatsApp lengkap dengan logic mask yang mengamankan bahwa angka awalan 0 selalu dikonversi menjadi 62.
  - **Update Label & Tombol**: Mengganti teks tombol aksi submit dari `Submit` menjadi `Register`.
- **Eksekusi Rencana Database Form Pendaftaran (`registration_table_plan.md`)**:
  - **Audit status database**: Tabel `registrations` di project Supabase `nusa-registration` sudah tersedia lengkap dengan seluruh kolom (`id`, `created_at`, `nama`, `no_whatsapp`, `pilihan_program`, `bukti_transfer_url`, `pernyataan_setuju`, `status`) beserta RLS policy *Allow public insert* dan *Allow authenticated select* yang sudah aktif.
  - **Membuat Storage Bucket `payment_receipts`**: Bucket baru dibuat di Supabase Storage dengan batas ukuran file 10 MB dan tipe MIME terbatas pada `image/jpeg`, `image/png`, `image/webp`, `image/gif`, dan `application/pdf`. Bucket dikonfigurasi sebagai *private*. RLS policy ditetapkan: `anon` hanya boleh *INSERT* (upload), sementara `authenticated` (admin) boleh *SELECT* (baca semua file).
  - **Migrasi `onSubmit` dari WhatsApp ke Supabase (`registration-form-page.tsx`)**: Menghapus total alur redirect `window.open(wa.me/...)` yang sudah dihapus berdasarkan keputusan workflow di plan. Menggantikan dengan alur asinkron dua langkah: *(1)* Upload file bukti transfer ke bucket `payment_receipts` menggunakan Supabase Storage Client; *(2)* Insert data teks pendaftaran (`nama`, `no_whatsapp`, `pilihan_program`, `bukti_transfer_url`, `pernyataan_setuju`, `status`) ke tabel `registrations`. Menambahkan *error handling* terpisah untuk setiap tahap dengan menampilkan pesan error bergaya `Alert` merah di atas tombol Submit apabila salah satu tahap gagal.
  - **Memperbarui Success UI**: Mengubah judul *"Pendaftaran Terkirim!"* menjadi *"Pendaftaran Berhasil!"* dan memperbarui teks deskripsi dari instruksi kirim WhatsApp menjadi informasi bahwa *"Admin NUSA akan segera menghubungi kamu melalui WhatsApp untuk konfirmasi pendaftaran"*, sesuai dengan keputusan workflow yang telah dikonfirmasi di `registration_table_plan.md`.

## 2026-04-17
- **Audit & Redesain Form Registrasi (`registration-form-page.tsx`)**: Menghapuskan blok warna kuning pudar/cokelat di luar pedoman pada area kotak Panduan Infaq, menggantinya dengan tint transparan *Accent* `rgba(243,178,51,0.1)` berteks *Dark Base* murni. Menanamkan *Digital Grid Overlay* super halus (opacity 3%) pada state pendaftaran dan *Success State* guna meneruskan DNA futuristik Hero Section. Memoles *Program Selector* dengan efek interaktif *glow semi-3D* dan border aktif warna *Primary*. Merevisi warna teks tombol Submit menjadi *Dark Base* untuk kontras maksimal di atas emas, serta menyempurnakan palet ikon logo WhatsApp "Hubungi Ustadz Yasir" ke warna *Charcoal (#2B2B2B)* murni demi kesesuaian 100% dengan standar UI dan estetika pilar utama.

## 2026-04-18
- **Eksekusi Hasil Audit Registrasi (`registration-section.tsx` & `registration-form-page.tsx`)**: Menyelaraskan *Card Container* dan *Success State* dari putih hampa `#FFFFFF` menjadi varian White brand `#F7F7F2`. Mengubah background input form dan *Program Selector* ke warna *Surface* `#F0FAF7` agar seimbang dengan panduan warna. Memperhalus *Grid Overlay* dengan memberikan sentuhan *Neon Cyan* `rgba(142,243,231,0.2)`. Melakukan standarisasi mutlak pada opacity *Dark Base* (`#134146`) untuk teks muted, disabled, dan border. Menggantikan seluruh `fontFamily` *inline style* menjadi *utility class* Tailwind terpusat `font-work-sans`. Menurunkan hierarki *font weight* label dan *button* menjadi `font-semibold` sesuai dengan ketentuan. Menambahkan pancaran *Glow Neon Cyan* pada efek *hover shadow* *button* pendaftaran serta bingkai *Program Selector* untuk nuansa interaktif B2B *Tech Academy* yang elegan.

## 2026-04-19
- **Penyempurnaan Audit Registrasi**: Memperbaiki tombol "Kembali ke Beranda" pada *Success State* (`registration-form-page.tsx`) yang terlewat, dengan mengubah `font-bold` menjadi `font-semibold font-work-sans` agar konsisten dengan hierarki tipografi *button* lainnya di seluruh *website*.
- **Redesain Form Registrasi (Gaya Rushd PPDB)** (`registration-form-page.tsx`):
  - **Section Headers (A–D)**: Mengubah teks heading biasa menjadi *badge header block* berdesain lingkaran solid berwarna *Primary* (`#2C8970`) bertuliskan huruf indeks (A, B, C, D), dilengkapi *border-bottom* pemisah antar kelompok informasi agar tata letak lebih lega dan terstruktur.
  - **Ikon Representatif per Field**: Memperbarui komponen `TextInput` agar menerima *prop* `icon`. Setiap *field* kini dilengkapi ikon `lucide-react` yang relevan di sisi kiri: `User` (Nama Lengkap), `Phone` (No. WhatsApp), `MapPin` (Tempat Lahir & Alamat & Lokasi Sekolah), `Calendar` (Tanggal Lahir), `Building2` (Asal Kota), `School` (Sekolah Asal), `HelpCircle` (Sumber Informasi).
  - **Redesain Tombol Submit**: Menghapus tombol *Back* (outline) dan merombak tombol *Register* menjadi tombol dominan *full-width* pada mode mobile, berwarna *Primary* (`#2C8970`), berukuran lebih tebal (`py-7`), lengkap dengan ikon `Send` dan animasi hover yang solid.
  - **Tombol Reset**: Mengubah teks "Clear form" menjadi "Reset Form" dan memindahkannya ke kiri, sejajar dengan tombol Submit utama.
- **Penyempurnaan Field Nomor WhatsApp**: Mengubah desain *field* menjadi *single unified input* (satu kotak terpadu). Ikon smartphone (`Phone`) kini tampil langsung di dalam kotak *input* di sisi kiri, dan nilai awal input sudah otomatis terisi `62` agar pengguna tidak perlu mengetik ulang kode negara.
- **Migrasi Link CTA Daftar ke `/daftar`**: Mengganti seluruh tombol CTA "Daftar Sekarang" yang sebelumnya mengarah ke Google Form eksternal (`https://forms.gle/92AqMyaUs81tyhLRA`) menjadi halaman internal `/daftar`. Tiga komponen diperbarui: `hero-section.tsx` (tombol sekunder "Daftar Sekarang"), `program-100-days.tsx` (CTA bawah kartu gelap), dan `program-section.tsx` (CTA bawah roadmap jurusan). Atribut `target="_blank"` dan `rel="noopener noreferrer"` turut dihapus karena navigasi kini bersifat internal.
- **Implementasi Notifikasi Telegram Otomatis (`notify-telegram`)**:
  - **Supabase Edge Function `notify-telegram` (v3)**: Men-deploy Edge Function baru berbasis Deno yang menerima payload data pendaftar dari database trigger dan mengirimkan pesan notifikasi berformat rapi ke grup Telegram Admin NUSA secara otomatis. Fungsi dilengkapi verifikasi *Authorization Bearer* untuk memastikan request hanya diterima dari trigger internal.
  - **Database Trigger `on_registration_insert`**: Mengaktifkan ekstensi `pg_net` dan membuat trigger `AFTER INSERT` pada tabel `public.registrations`. Setiap kali ada pendaftar baru yang berhasil menyimpan data, trigger memanggil Edge Function via `net.http_post()` secara asinkron tanpa memblokir proses simpan data. Trigger dilengkapi blok `EXCEPTION` sehingga kegagalan notifikasi tidak mengakibatkan kegagalan pendaftaran.
  - **Format Pesan Notifikasi**: Pesan yang diterima Admin berisi: Nama Lengkap, Nomor WhatsApp, Tempat & Tanggal Lahir, Kota, Alamat, Sekolah Asal, Pilihan Program (Programmer / Designer), Sumber Informasi, Status, dan Waktu Daftar (WIB).
  - **Verifikasi**: Test end-to-end via cURL menghasilkan HTTP 200 OK dan pesan Telegram berhasil diterima di grup Admin. Edge Function logs di Supabase Dashboard mengonfirmasi eksekusi berhasil dalam ~2 detik.
  - **Dokumentasi**: Rencana implementasi tersimpan di `docs/telegram_notification_plan.md`.

## 2026-04-29
- **Implementasi Halaman Tes Kecenderungan Programmer vs Designer (`/test`)**:
  - **Pembuatan Komponen**: Membangun `TestProgrammerDesigner` di `components/test-programmer-designer.tsx` yang mencakup form input nilai tes dari *brght.org*, *cantunsee.space*, dan *color.method.ac*.
  - **Logika Penilaian**: Menerapkan formula normalisasi skor dan kalkulasi deviasi (`delta`) sesuai dengan yang didefinisikan pada dokumen MVP `docs/test-programmer-designer-plan.md` untuk menghasilkan 5 klasifikasi kecenderungan (Strong Programmer, Programmer, Balance, Designer, Strong Designer).
  - **Desain UI/UX**: Menggunakan desain gaya *Tech Academy* dengan elemen *Glassmorphism*, variasi warna resmi (Primary `#2C8970`, Secondary `#42CDBA`, Accent `#F3B233`, Surface `#F0FAF7`, dan Dark Base `#134146`), serta menggunakan ikon dari `lucide-react` untuk memperkaya antarmuka pengguna.
  - **Routing**: Mengintegrasikan komponen ke dalam route *Next.js* baru pada `app/test/page.tsx` lengkap dengan Header utama NUSA Boarding School.
